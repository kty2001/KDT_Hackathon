# 밤마실 — 현재 상태 한 장

> **이 파일만 읽고 작업을 시작할 수 있게** 유지한다. 다른 문서는 *필요할 때만* 연다.
> 갱신: **2026-08-13** · 작업을 끝낼 때마다 이 파일을 먼저 고칠 것.

야맹증 저시력자의 야간 보행을 돕는 **실시간 AI 시각보조**(스마트폰 단독).
`카메라 → ① 눈부심 억제 → ② 저조도 개선 → ③ 위험요소 탐지 → ④ 선택적 강조 → 화면`.
목표 **720p 15FPS**. 해커톤 마감 **9월 초**.

실행은 전부 `uv run python scripts/xxx.py` (→ [README](../README.md#개발-환경)).

---

## 1. 단계별 현재 상태

### ★ 아키텍처 확정 — 표시/탐지 경로 분리 (8/2, `C7`)

```
프레임 ─┬─→ [탐지 경로] 원본 그대로 ────────→ ③ YOLO ──┐
        │                                              ↓
        └─→ [표시 경로] ② D1A1+bf(+ts) ──────→ ④ 경계선 강조 → 화면
```

②를 탐지 앞단에 두면 mAP 가 내려가고, 무엇보다 **`stairs` 야간 오탐이 0.1% → 5.7%
로 되살아난다**(`C4b` 가 0 으로 만든 결함이 전처리 한 번에 복귀). 그래서 **탐지는 원본을
먹는다.** 분리는 비용이 아니라 이득이다 — 두 경로가 독립이라 병렬화 가능하고 ②의 지연이
③에 더해지지 않는다 (→ [detection.md 7장](detection.md)).

| 단계 | 채택안 | 확정된 수치 | 상태 |
|------|--------|-------------|------|
| ① 눈부심 억제 | `D1`(Drago) 하이라이트 압축 — **②에 흡수 예정** | 광원 코어 250→157 @640×360 | 🗣️ 통합 여부 회의 안건 |
| ② 저조도 개선 | **고전 CV 확정**(8/1) · **표시 경로 전용**. `D1A1+bf` + **`ts`(플리커)** | 3축 만족 · `+ts` 증분 **+1.5ms** | ⚠️ **속도 게이트 경계** (≈20.4ms@640×360) |
| ③ 위험요소 탐지 | **YOLO11n 단일 모델** · **입력은 원본** · 클래스 **`0 person` `1 stairs` `2 bollard`**(8/3 확정) | held-out mAP50 **0.684** · recall **0.609** · `stairs` 오탐 **0.2%**(rec34 2장)·0.0%(rec38) | ✅ person·stairs 완료 (8/2) · 🟡 **bollard 는 외부에서 학습됐다 — `c4d` 6런 (8/7)**. 채택 전 (→ ★인수분) |
| ④ 선택적 강조 | 이중 스트로크 비채움 렌더 | 박스당 **0.5ms** | ✅ 구현 · 실기기 검증 대기 |
| **파이프라인 통합** | `scripts/pipeline_demo.py` | 데스크톱 end-to-end 동작 확인 (8/2) | ✅ 사전 검증 · 앱 이식 대기 |
| **③ 앱 전달물** | ONNX FP32 · 640 고정 · NMS 제외 | `outputs/export/bammasil_det_c4b_loli0_640.zip` (9.2MB) · PT↔ONNX 일치 확인 | ✅ 공유 가능 (8/3) |
| 앱 | — | — | ❌ **구현 0건** |

**채택 가중치**: `outputs/detect/c4b_loli0/weights/best.pt`
(`c4b_loli6000`(0.691/0.625/오탐 0.0%) 과 사실상 동점 — 학습 비용 절반 · pseudo-label
의존 없음. ⚠️ 요약 수치는 **채택 런 기준**으로 적을 것 — 두 런의 수치를 섞어 쓰면
"오탐 0.0%" 같은 과대 표기가 된다 → [detection.md 6-7](detection.md))

---

## ★ 외부 인수분 — `received/` (8/7 도착 · 이 저장소에서 미재현)

> 번호 없는 절이다. 다른 문서가 `STATUS 2장`·`3장 함정 8` 로 참조하고 있어
> **기존 절 번호를 밀지 않았다.** 여기 소절은 `R1`~`R5` 로 가리킨다.

별도 환경에서 돌린 **`C4d` — 3클래스 6런 모델 비교**가 `received/` 로 들어왔다.
2장 5번의 `C4c` 를 이어받은 결과이고, 우리 `outputs/detect/`(`c4b_*`)와 겹치지 않는다.
**git 미추적** · **코드는 오지 않았다**(표·CSV·가중치·오류 사례만) — 아래는 전부 **인용**이다.

**원문**(진입점은 `received/README.md`, 10장이 회신 요청) → 문서 지도 5장.
가중치는 `outputs/bammasil_results/bammasil_weights/c4d_*/best.pt` 에 풀려 있다.
여기 R1~R5 는 **결정에 필요한 것만** 남긴 요약이다.

### R1. 6런 결과 — 공식 지표는 `benchmark_c4d.csv`

평가셋 **`bollard_val_2146`**(2,146장 · person 4,736박스 · **bollard 6,988박스**).
`Bbox_15_new` 단독 배치로 **학습에 한 번도 안 쓰였고 누수 확인됨**.
공통 조건: 11,000장 축소셋 · patience 10 · batch 16 · seed 0.

| 런 | 모델 | 입력 | 배경 | ep | inf ms | NMS | mAP50 | bollard R | person R | bollard FP |
|---|---|---|---|---|---|---|---|---|---|---|
| c4d_11n_640 | YOLO11n | 640 | 10% | 50 | **1.00** | NMS | 0.753 | 0.619 | 0.687 | 1,221 |
| c4d_26n_640 | YOLO26n | 640 | 10% | 50 | 1.31 | NMS-free | 0.775 | 0.656 | 0.694 | 925 |
| c4d_11s_640 | YOLO11s | 640 | 10% | 50 | 1.57 | NMS | 0.812 | 0.674 | 0.752 | 953 |
| **c4d_26n_960** | YOLO26n | 960 | 10% | 48★ | 2.32 | NMS-free | **0.840** | **0.711** | **0.786** | **792** |
| c4d_11n_640_noneg | YOLO11n | 640 | **0%** | 50 | 1.28 | NMS | 0.759 | 0.638 | 0.697 | 1,361 |
| c4d_11n_960 † | YOLO11n | 960 | 10% | **30†** | 2.08 | NMS | 0.833 | 0.710 | 0.757 | 1,143 |

★ patience 10 조기종료 · † **30ep** — 학습량이 달라 **순위 비교에 넣지 말 것**
`inf ms` 는 **데스크톱 RTX** 값이다. 실기기가 아니다.

**세 가지가 확정됐다.**

1. **해상도가 모델 세대보다 크다** — 동일 50ep YOLO26n 축에서 해상도 **+0.065** vs 모델
   **+0.022**(3배). **960 의 비용은 볼라드에서 발생하지 않는다**(FP 925→792). 단
   해상도는 박스 부정확은 고쳐도 헛보기는 못 고치고, ⚠️ **person 만은 오히려 는다.**
2. **배경 이미지 10% 유지** — `noneg` 는 mAP·recall 이 **좋아 보이지만** 오탐이 6런 중
   최다다(볼라드 FP +11% · person +16%). **효과가 mAP 가 아니라 FP 에 나타난다** —
   mAP 만 보고 뺐으면 오판이었다.
3. **운영 conf 는 0.10 이 무릎** — 0.25→0.10 에서 bollard **+7.4~8.5%p 에 FP 2.6~3.2배**,
   0.01 은 프레임당 오탐 12~18개로 ④ 가 화면을 덮는다. 미탐이 오탐보다 위험하므로
   하향은 정당한 카드 — FSM cooldown 과 함께 본다.

`stairs` 는 이 평가셋에 GT 0개라 개발 val 로만 쟀고(0.985~0.991 포화), **6,988박스에서
FP 2건**뿐이었다. ⚠️ 다만 **주간 근거다** — 실야간에서는 뒤집힌다(2장 4 · 8/13).
`confusion`(person↔bollard)은 1% 미만이라 **클래스 설계는 건전하다.**

### R2. ★ 이 수치는 전부 **주간**이다 — `is_night` 은 무효 필드

- `is_night` 은 **파일 mtime**(추출·복사 시각)에서 나왔다 — 한 세션 mtime 범위 **584일** ·
  프레임 순서 **역행** · EXIF 촬영시각 **0**. `is_night=1` 로 고른 2,146장이 **99.1% 주간**.
- AIHub 인도보행 **실측 야간은 220/37,961 = 0.58%**. 볼라드 보유 12,806장 중 야간 **47장**,
  누수 없는 것은 **14장** — 통계가 성립하지 않는다. **→ 야간 볼라드는 미측정 항목.**

> **야간 근거의 사정거리**: NightOwls·StairNet 은 `person`·`stairs` 한정이다
> (NightOwls 에 볼라드 라벨이 없고 StairNet 은 계단 데이터셋).
> **`bollard` 의 야간 근거는 자체 촬영뿐이다** — 8/13 에 실촬영 7장으로 첫 정성 소견이
> 나왔으나 라벨이 없어 여전히 미측정이다 (2장 5).

★ **2장 5번의 "AIHub 는 주간 전용인가"에 답이 나왔다** — "저조도 ~600장"은 **야간이
아니었다.** 8/5 정찰의 "밝기와 광원은 별개 축" 가설이 맞았다. 다만 인수분은 **휘도만**
쟀으므로 `--dry-run` 의 **광원 연결성분**·**블록 수 집계**는 남는다.

### R3. 이 문서의 기존 서술과 어긋나는 것

| 항목 | 기존 (8/5) | `received/` (8/7) |
|---|---|---|
| AIHub 야간 여부 | 🔴 "검증 중 — 저조도 ~600장 보고" | **결론: 야간 아님.** 실측 야간 0.58% · 누수 없는 야간 볼라드 **14장** |
| bollard 성능 전망 | "640 에서 절반이 탐지 하한 아래" | 방향은 맞음. bollard mAP50 0.75~0.83 달성. 다만 **`<4px`(GT의 22%)는 960 에서도 recall 0.49 가 상한** — 해상도로 더 못 간다(1280·타일링 영역) |
| `C2` 우선순위 | "정찰 결과에 따라 달라진다" | **더 올라간다.** 야간 볼라드를 잴 수단이 자체 촬영뿐임이 확정됐다 |
| 채택 가중치 | `c4b_loli0` (2클래스) | c4d 는 **별개 계보**(11K 축소셋·3클래스). **아직 `c4b` 를 대체하지 않는다** → R4 |

### R4. ⚠️ c4d 를 채택안으로 올리지 말 것 — 아직 판정되지 않았다

- **6런 모두 held-out 야간 평가를 한 번도 통과하지 않았다** (`/mnt/d` 마운트 해제로
  `eval_nightowls`·`eval_stairs_night` 미실행). **3장 함정 1 그대로다.**
- **학습셋(11K)이 "야간 위주"라는 전제가 흔들린다** — 선별이 전부 무효 필드 `is_night`
  기반이었다. 2장 5번의 "합치면 주간 90% → `C4` 붕괴 재현"이 **아직 살아 있다.**
- **실기기 속도 미측정** — 960 은 픽셀 2.25배. **배포 해상도는 640 유지**가 현재 판단이다.
- **코드가 없어 재현 불가.** ⚠️ 인수분 안에서도 `results.csv`(학습 중 마지막 epoch) ≠
  **`benchmark_c4d.csv`(공식값)** 다. `fig1_curve.png` 도 공식 지표가 아니다.

### R5. 회신 대기 3건 (→ `received/README.md` 10장)

1. 🔴 **야간 데이터셋 사양** — 장수·조건·평가전용/학습포함·클래스 범위. 보낸 쪽은
   **당일 촬영·라벨링 가능**이라 했고, 구간별 recall 에는 **볼라드 수백 박스**가 필요하다.
   → **우리 `C2` 와 정면으로 겹친다. 같은 밤을 두 번 찍지 않도록 분업부터 정할 것.**
2. **ONNX 로 넘길 비교축 런 지정** — `c4d_11n_640`(기준)·`c4d_26n_640`·`c4d_26n_960`.
   ⚠️ **YOLO26n 은 NMS-free 라 앱 인터페이스 계약이 바뀐다**(raw 출력 + 앱측 NMS).
   기존 배포물은 NMS 제외 FP32 640 이므로 계약 변경 공지가 필요하다.
3. **KPI 비교표** — 실기기 640 계측(p50 32.7 · p95 38.1ms · 11분)이 나오면 960·26n 과 대조.

---

## 2. 지금 막혀 있는 것 — 우선순위

1. 🔴 **`C2` 자체 야간 촬영** — `C3`→`C5`→`C7`→`C8`→`C9` 가 전부 직렬로 매달려 있다.
   ✅ **선행 조건(회의 안건 3·4)은 8/3 해소** — 개인정보는 "안 걸리게 촬영", `stairs`
   라벨 정의는 확정(→ [labeling_stairs.md](labeling_stairs.md)). **막는 것이 없다.**
   - ⚠️ 다만 **안건 7(일반 장애물)** 이 미결인 채 남아 있다. 추가로 결정되면 촬영 코스에
     장애물 구간이 필요해 **재촬영 위험**이 있다 — 나가기 전에 같이 처리할 것.
   - ⚠️ 촬영 시 **멀리서 계단에 접근하는 구간**을 반드시 넣을 것. 가까이서만 찍으면
     `stairs` 의 최대 미검증 항목(거리 계단)이 이번에도 측정 불가로 남는다.
   - 🆕 **볼라드 구간이 이제 필수다** (→ ★인수분 R2). AIHub 로는 야간 볼라드를 영영 못 잰다.
   - 🆕 **인수처도 야간 촬영을 하겠다고 회신을 기다리고 있다** (→ ★인수분 R5).
     같은 밤을 두 번 찍지 않도록 **사양·분업을 먼저 정할 것.**
2. 🔴 **`P3` 앱 껍데기** — 구현 0건. `C9` 통합 시점에 없으면 통합할 곳이 없다.
3. ⚠️ **② 속도 게이트** — 720p 를 확실히 통과하는 건 `A1`·`A2`(6ms) 뿐인데 **이 둘은
   글레어 축 실격**이다. 채택안 `D1A1+bf+ts` 는 640×360 환산 **≈20.4ms** 로 게이트
   경계선(판정 불가 밴드 15~25ms). `bf` 를 A3 시간축으로 대체하려던 카드는 **8/2 에
   기각**됐다 (→ lowlight_classical 8장). 남은 수: 내부 해상도 하향 · 셰이더 이식 ·
   `C11` 실기기 재판정.
   - ✅ **다만 표시/탐지 분리로 압박이 줄었다** — ②의 지연이 ③에 더해지지 않으므로
     두 경로를 병렬로 돌리면 예산 계산이 달라진다. 🗣️ 회의 안건 1에 반영할 것.
4. **`stairs` — 저조도는 문제가 아니고 *도메인*이 문제다** (8/2 실측).
   StairNet 야간 76장에서 mAP50 **0.993** · recall 0.987 로 **주간보다도 높다.**
   그러나 StairNet 은 *계단이 화면을 채운 사진*이라, **보행 중 멀리 있는 계단**은
   측정 자체가 불가능하다. 야간 거리 오탐 0.2% 는 conf 스윕(0.25→0.01 에서도 6.1%,
   최대 conf 0.396)으로 **"헛보지 않는다" 쪽에 가깝다**고 봤지만, rec 34 엔 진짜 계단이
   없어 **"거리 장면에선 아예 발화하지 않는다"** 를 배제하지 못한다
   (→ [detection.md 8장](detection.md)).
   - ★ **판별 수단이 하나 늘었다 (8/4)** — AIHub 노면 마스킹 `stairs` 는 **1인칭 보행
     시점**이라 "원거리 계단"을 처음으로 잴 수 있다. **아직 안 받았다**(→ TODO `W5`).
     ⚠️ 주간 위주로 보이나 이 역시 **샘플 근거**다 — bbox 태스크에서 그 전제가 흔들렸으므로
     (2장 5) 받으면 `--dry-run` 정찰로 같이 확인한다. 야간 계단은 일단 `C2` 뿐이다
     (→ [detection.md 8-3](detection.md) · [data.md 3-1-2](data.md)).
   - ✅ **"헛보지 않는다"가 한 번 더 확인됐다** — 인수분의 `bollard_val_2146` 6,988박스에
     `stairs` GT 가 0개인데 FP 가 **2건**뿐이었다 (→ ★인수분 R1).
   - 🔴 **그런데 실야간 노면에서는 되살아난다 (8/13 실측).** `data/test_real_data/` 7장에
     `c4d_11n_640` 을 물리니 **계단이 없는 장면**(자전거도로·점자블록·하천 바위)에서
     `stairs` 가 나오고, 동영상 4프레임은 **전부** 발화했다. 반대로 **진짜 계단은
     원본에서만** 잡히고 `gain 0.35` 부터 0개다. 위 두 ✅ 는 **대시캠(rec34)** 과
     **주간 AIHub** 에서 얻은 것이라, **보행 시점 실야간에는 사정거리가 닿지 않는다.**
     n=7 이라 판정은 아니지만 방향은 `C7`(②를 앞단에 두면 0.1%→5.7%)과 같다
     (→ [detection.md 8-4](detection.md) · [notebook 9절](../notebooks/aihub_subset_review.ipynb)).
5. 🟡 **`bollard` — 데이터는 들어왔고, 이제 *어떻게 넣느냐*가 문제다** (8/5 · 8/7 갱신).
   클래스 `0 person`·`1 stairs`·`2 bollard` 확정(8/3) · 배선 완료 · **AIHub bbox 전량
   300GB 다운로드 완료** → "샘플에 172박스뿐" 병목 해소. 그러나 **장수를 그대로 학습에
   넣으면 안 된다.** 학습 전 4단계와 게이트는 → [TODO `C4c`](TODO.md), 근거는
   → [data.md 3-1-4](data.md).
   ★ **`C4c` 는 외부 환경에서 먼저 실행됐다** — 결과는 ★인수분. 아래는 그에 비춘 현황이다.
   - ✅ **"AIHub 는 주간 전용인가"에 답이 나왔다** (→ ★인수분 R2). 전량 1/30 의 "저조도
     ~600장"은 **야간이 아니었다** — 실측 야간 **0.58%**. 8/5 정찰의 "밝기와 광원은 별개
     축" 가설이 맞았다. **`C2` 우선순위는 내려가지 않고 올라간다.**
     ⚠️ 인수분은 **휘도만** 쟀으므로 `--dry-run` 의 **광원 연결성분**·**블록 수** 는 남는다.
   - ⚠️ **장수 ≠ 장면 수** (연속 프레임) · **전량을 합치면 주간이 90%** 가 되어 `C4`
     붕괴 조건이 재현된다 · **야간이 실재하면 그 블록을 다 학습에 넣지 말 것**
     (rec 34 에 볼라드 라벨이 없어 잴 자가 사라진다).
     → 인수분은 11K 축소셋으로 피해 갔지만 **held-out 야간 평가를 못 돌려 이 항목은 미해결**이다.
   - 🆕 **야간 볼라드에 정성 근거가 처음 생겼다 (8/13).** `test_real_data/_04` 한 장뿐이지만
     `c4d_11n_640` 이 볼라드 2개를 **세 밝기 전부**에서 잡았다. 다만 어두워질수록
     confidence 가 무너진다 (가까운 쪽 **0.55 → 0.44 → 0.32**). 라벨이 없어 recall·FP 는
     못 세므로 R2 의 "미측정" 상태는 그대로다 — **`C2` 가 여전히 유일한 판정 수단**이다.
     이 confidence 붕괴는 운영 conf **0.10**(R1) 을 지지하는 쪽이다.
   - ✅ **크기 구간별 recall 은 뽑혔다** — 폭 중앙 **7.8px** · 8px 미만 **50.3%** 라는 우려는
     방향이 맞았고, **`<4px`(GT의 22%)는 960 에서도 recall 0.49 가 상한**이다.
     이 구간은 해상도로 더 못 간다 — 1280 또는 타일링 영역 (→ ★인수분 R1).
   - ★ **`stairs` 도 AIHub 에서 나온다** (8/4 정정) — 8/3 의 "계단 없음"은 **장애물 태스크
     한정**이었고, **Surface Masking(노면 20종)에 `caution_zone > stairs`** 가 있다.
     클래스 1 의 소스 추가라 배선을 안 건드린다. **아직 안 받았다**(→ TODO `W5`).
   - 🗣️ **클래스 2 의 범위가 열려 있다** — 2장의 "전봇대"는 AIHub `pole`(높이 중앙 648px)
     이고 `bollard`(75px)와 별개다. 현재는 좁은 정의 (→ 회의 안건 7).
     ★ **판단 재료가 생겼다** — 인수분의 볼라드 FP 중 `ghost` 가 **57~63%** 이고 그 정체가
     **기둥·가로등·표지판 지주**다. 범위를 넓히면 이 FP 가 정탐으로 바뀌는지 먼저 따질 것.

---

## 3. ⚠️ 반복해서 발목을 잡은 함정 — 작업 전 반드시 확인

1. **개발 val 은 야간 실효 성능을 못 본다.** `C4b` 에서 실야간이 5.4배 좋아지는 동안
   개발 val 은 0.892 → 0.892 로 **한 자리도 안 움직였다.** 판정은 **항상**
   held-out(NightOwls rec 34) 또는 자체 촬영분(`C5`)에서 한다.
2. **지표를 영상 통계에 상대적으로 정의하지 말 것.** 백분위·평균 대비로 정의하면
   영상이 바뀔 때 *재는 대상 자체*가 달라진다. 노이즈·글레어·대비 3개 지표가 전부
   이 결함으로 육안과 어긋났다 (→ `scripts/metrics.py`).
3. **이 PC 는 15~25ms 를 판정할 수 없다.** 프로세스 간 10~20% 흔들린다.
   다른 작업(학습 등)과 동시에 재면 **1.7배까지 부풀어 오른다.**
4. **NightOwls `ignore`-only 프레임 3,097장은 학습에 넣지 말 것.** 사람이 있는데
   라벨이 비어 있어 "사람=배경"을 가르친다 (→ `scripts/nightowls_yolo.py`).
5. **NightOwls rec 34·38 은 학습 금지.** 34 는 판정용 held-out, 38 은 ② 시간축 전용.
   `build_detect_dataset.py` 가 실행을 거부하도록 막아 뒀다.
6. **Windows 에서 `--cache ram` 금지.** worker 가 spawn 이라 캐시가 복제돼 죽는다.
7. **Raw 데이터 수정 금지.** 산출물은 전부 `outputs/`(git 비추적), 이미지는 하드링크.
8. ★ **AI Hub 는 해외 IP 다운로드를 차단한다** (8/4 실측). Colab·해외 클라우드에서
   `aihubshell` 을 돌리면 `502 · "해외에서의 데이터 다운로드를 제한"` 으로 끝난다.
   우회는 이용정책 위반이라 하지 않는다 — **AIHub 취득은 국내 PC 에서만 가능**하고,
   클라우드 GPU 를 쓰려면 **국내에서 받아 640 리사이즈해 옮기는** 경로뿐이다
   (`scripts/aihub_pack_for_colab.py` → [data.md 3-1-3](data.md)).
   같은 실측에서 하나 더 — **라벨과 원천이 안 갈려 있다.** 태스크별 10GB 통zip 뿐이라
   "라벨 먼저 받아 분포 실측" 이 불가능하고, **태스크별 1번 zip 정찰**로 대체한다.
9. **ultralytics 의 AutoUpdate 를 켜 둔 채 ONNX 추론을 돌리지 말 것** (8/3 실측).
   `onnxruntime`(CPU) 이 없으면 **제멋대로 설치**해 `uv.lock` 밖 패키지를 끼워 넣는다.
   이 프로젝트는 `onnxruntime-gpu` 를 쓰는데 **둘은 같은 디렉토리를 공유**해서, 나중에
   CPU 판을 지우면 공용 파일까지 사라져 `onnxruntime` 이 통째로 망가진다
   (`module has no attribute 'InferenceSession'`). 복구는 `uv sync --reinstall-package
   onnxruntime-gpu`. 예방은 임포트 전 `YOLO_AUTOINSTALL=false` (→ `scripts/export_onnx.py`).
10. **`.pt` 와 ONNX 를 같은 이미지로 비교할 때 파일 경로를 그대로 넘기지 말 것** (8/3).
   ultralytics 는 `.pt` 에 **가변 rect 레터박스**(16:9 → 640×384), 고정 shape ONNX 에는
   640×640 을 먹인다. 입력 해상도가 달라 비정사각 이미지에서 신뢰도가 0.1 씩 벌어지는데
   이는 **변환 손실이 아니라 전처리 차이**다. 미리 정사각 레터박스한 배열을 양쪽에
   똑같이 넣으면 좌표 오차 0.0001px 로 일치한다.
11. ★ **다른 머신으로 옮길 데이터셋의 `data.yaml` 에는 `path:` 를 넣지 말 것** (8/4 실측).
    ultralytics 는 `path:` 가 **상대경로면 `settings['datasets_dir']` 기준**으로 푼다
    (Colab 은 `/content/datasets`). 로컬에서는 우연히 맞아떨어지지만 zip 을 옮기는 순간
    `images not found, missing path '/content/datasets/outputs/datasets/...'` 로 죽는다.
    절대경로를 넣으면 이번엔 Windows 경로가 박혀 역시 못 쓴다. **`path:` 를 빼면
    ultralytics 가 yaml 파일의 위치를 기준으로 잡아** 어느 머신에서든 동작한다
    (`aihub_pack_for_colab.make_portable`). 로컬 전용 데이터셋은 지금처럼 절대경로여도 된다.
12. ★ **한국어 Windows 에서 조용히 죽는 것 2개** (8/5 실측). 둘 다 **큰 데이터를 다 훑은
    뒤**에 터져서 실행이 통째로 날아간다 — 긴 작업 전에 확인할 것.
    - **콘솔이 cp949 라 `⚠️`·`★` 출력에서 `UnicodeEncodeError`.** 대비는
      `sys.stdout.reconfigure(errors="replace")` (한글은 그대로, 이모지만 흘린다).
      `chcp 65001` 을 요구하지 않는 쪽이 낫다 — 팀원이 한 번 돌리는 스크립트다.
    - **`cv2.imread` 가 비ASCII 경로를 못 연다.** `data/AIHub인도보행영상…` 처럼 한글이
      섞이면 예외 없이 **전부 `None`** 이 되어 "한 장도 못 읽었다"로 끝난다. 대비는
      `cv2.imdecode(np.fromfile(path, np.uint8), ...)`. PIL 은 영향 없다.
13. ★ **파일 mtime 을 촬영시각으로 쓰지 말 것** (8/7, 외부 인수분에서 발생 → ★인수분 R2).
    AIHub 야간 선별이 `mtime` 기반 `is_night` 으로 돌아가 **99.1% 가 주간인 셋을 "야간"으로**
    쓰고 있었다. mtime 은 추출·복사 시각이고 EXIF 촬영시각은 아예 없다.
    **야간 판정은 실측 휘도로 한다** — 이 절 2번과 뿌리가 같다(재는 대상이 딴것이 된다).

---

## 4. 데이터 — 무엇이 어디에

| 데이터셋 | 규모 | 성격 · 주의 |
|---|---|---|
| **NightOwls** | 51,848장 중 **13,602장 해제** | ★ 사람 손 라벨 · **전량 야간**. 유일한 정직한 야간 평가원. **차량 대시캠**이라 보행 시점과 다름. `D:\datasets\NightOwls` → `data/NightOwls` Junction |
| LoLI-Street | 33,000쌍 (+person 라벨) | ⚠️ **주간을 어둡게 만든 합성본.** 포화 광원 **0.00%** → 글레어를 학습·측정 불가. low/high 라벨 불일치(high 가 +24.8%) |
| StairNet | 3,094장 (야간 505) | `stairs` 유일 소스. **계단 중심 사진**이라 음성 표본이 없음 |
| ExDark | 7,363장 | 실제 야간이나 **실내가 많음** |
| LOL | 500쌍 | 벤치마크 비교용 |
| **AIHub 인도보행** | bbox 전량 300GB 확보(8/5) | `bollard` 유일 소스 · **주간 데이터셋**(실측 야간 **0.58%** — 8/7 확정, → ★인수분 R2) |

**NightOwls recording 배치 (고정)**: train `36·none·37` / val `35` / **test `34`** / 제외 `38`

**실촬영 테스트 소재**: `data/test_real_data/` (야간 실촬영 사진 7 + 동영상 5, 8/8 · 세로
3060×4080). `lowlight/`(gain 0.35) · `lowlight_x02/`(gain 0.2) 는 `scripts/darken.py` 로
**한 단계씩 더 어둡게 합성**한 것(선형 노출 감소 + 센서 노이즈) — ② arm 의 하한을 눈으로
보는 **정성 확인용**. 합성본이므로 지표 근거로 쓰지 말 것.
★ **라벨은 없지만 내용이 값나간다** — `_04` **야간 볼라드 2개**(프로젝트 유일) · `_05` 계단 ·
나머지 5장은 계단·볼라드가 **없는** 야간 노면이라 **오탐을 볼 음성 표본**이다. **사람은 0장.**

---

## 5. 문서 지도 — *필요할 때만* 연다

| 알고 싶은 것 | 문서 | 크기 |
|---|---|---|
| **지금 뭘 해야 하나** (순서·병렬성·일정·담당) | [TODO.md](TODO.md) | 20KB |
| ★ **외부 인수분 원문** — c4d 6런 비교 · 야간 측정 불가 경위 · 회신 요청 (→ ★인수분) | [../received/README.md](../received/README.md) · [RESULT](../received/bammasil_docs/RESULT_20260807.md) | 9·17KB |
| 기획 (배경·구성·일정) · 환경 셋업 | [../README.md](../README.md) | 24KB |
| ③ 탐지 — 데이터 구성 · `C4`/`C4b` 판정 · 계단 도메인 | [detection.md](detection.md) | 40KB |
| ② 고전 arm — 지표·순위·속도·A3 시간축 | [lowlight_classical.md](lowlight_classical.md) | 60KB |
| 데이터 상세 · **AIHub**(3-1) · 계단 방식 근거 | [data.md](data.md) | 56KB |
| 모바일 런타임·양자화·FPS 병목 | [hardware_inference.md](hardware_inference.md) | 8KB |
| 스크립트가 뭘 하는지 | [../scripts/README.md](../scripts/README.md) | 8KB |
| ★ **`stairs` 라벨을 어떻게 치는가** (`C3` 작업자용) | [labeling_stairs.md](labeling_stairs.md) | 12KB |
| ★ **`bollard` 를 어떻게 넣는가** (`C4c` 착수 전 필독) | [data.md 3-1-4](data.md) | — |
| ★ **계단 탐지가 완료됐는가** (그림으로) | [../notebooks/stairs_night_review.ipynb](../notebooks/stairs_night_review.ipynb) | — |
| ★ **AIHub 를 받아 학습까지** (Colab · GPU 없는 PC 에서도) | [../notebooks/colab_aihub_train.ipynb](../notebooks/colab_aihub_train.ipynb) | — |
| ★ **c4d 를 눈으로 확인** — 라벨 검수(1~7절) · **c4d 640 추론**(8절) · **실야간 추론**(9절) | [../notebooks/aihub_subset_review.ipynb](../notebooks/aihub_subset_review.ipynb) | — |
| ★ **기획서 v20 수치 검증** — 그림 8 · 2.4 KPI · 3.2 · 3.3 (수정 필요 9건) | [review_proposal_v20_20260808.md](review_proposal_v20_20260808.md) | 12KB |
| 팀 공유 — ③ 훈련 결과 요약 + 앱팀 인수인계 | [share_yolo_c4b_20260803.md](share_yolo_c4b_20260803.md) | 12KB |
| 목적 축을 **무엇으로 어떻게 재는가** | [../scripts/metrics.py](../scripts/metrics.py) | — |
| 종결된 실험·결정 원문 | [archive/](archive/) | — |

> 📌 **큰 문서는 통째로 읽지 말 것.** 각 문서 맨 위 `0. 결론 요약`만 읽고,
> 필요한 절만 `grep -n "^#" 파일` 로 찾아 `sed -n 'A,Bp'` 로 부분 열람한다.

---

## 6. 최근 완료 (최근 며칠)

전체 완료 이력은 → [TODO 8장](TODO.md). 여기는 **아직 머릿속에 있어야 하는 것**만 둔다.

| 항목 | 결과 |
|---|---|
| ★ **c4d 를 눈으로 확인 — 실야간 첫 소견** (8/13) | `aihub_subset_review.ipynb` 에 라벨 검수(1~7절) + **c4d 640 추론**(8절) + **실야간 추론**(9절)을 붙였다. 🆕 **야간 볼라드가 잡힌다**(세 밝기 전부, conf 0.55→0.32 붕괴) · 🔴 **`stairs` 오탐이 실야간 노면에서 되살아난다**(계단 없는 5장·동영상 4/4 프레임) · ⚠️ **진짜 계단은 원본에서만** 잡힘. 라벨이 없어 **판정 아님**(n=7) — `C2`·`C3` 로 넘길 단서다 (→ 2장 4·5 · [detection.md 8-4](detection.md)) |
| ★ **기획서 v20 수치 검증** (8/8) | 그림 8 5개 점·사전 기준·데이터 규모는 **공식 CSV 와 일치** ✅. 🔴 **수정 필요 5건** — 배포 설정 모순 · 볼라드 mAP50 이 인수분 안에서 **0.757 vs 0.775 충돌** · `person 47,791` 은 폐기 라벨 수치 · `12,855박스` 출처 불명 · **"무처리 32.7ms"로 "15FPS 여유" 결론**. 🟡 계단 오탐 `7.7→0.2` 의 귀속도 틀렸다(대조군이 반증) — 항목별 원문 → [review](review_proposal_v20_20260808.md) |
| ★ **외부 인수분 `received/` 도착** (8/7) | `C4c` 를 이어받은 **`C4d` 3클래스 6런 비교**. **`c4d_26n_960` 이 정확도·오탐 양쪽 1위**(mAP50 0.840) · **해상도 효과가 모델 세대의 3배** · 배경 10% 는 mAP 가 아니라 **FP 에서** 정당화 · 운영 conf 무릎 **0.10**. ★ **AIHub 의 `is_night` 이 mtime 기반 무효 필드**로 밝혀져 **야간 볼라드는 측정 불가 확정**. 다만 held-out 야간 평가 미실행이라 **채택 전** (→ ★인수분) |
| ★ **정찰 — 저조도 판별 · 촬영 블록 수** (8/5) | `aihub_pack_for_colab.py --dry-run` 이 **①몇 블록인가 ②저조도가 야간인가 그늘인가**를 낸다. ★ **밝기와 광원을 별개 축으로 잰다** — 임계는 보유 데이터로 보정했고(주간 p5 **94.4** / LOL low p95 **31.0** 사이가 비어 절대 임계가 안전), 요점은 **LOL low 의 포화 광원이 0.00%** 라는 것이다. LoLI 와 같은 부류라 "어둡다"와 "야간이다"가 다른 축임이 실측으로 확인됐다. 광원 **연결성분**으로 하늘(큰 덩어리)과 가로등(작은 점 여럿)을 가른다. 3개 분기 전부 검증(샘플 → 주간 98.3%·1블록 · LOL low → 저조도 97.5%·광원없음 · 합성 점광원/하늘). 근거·읽는 법 → [data.md 3-1-4](data.md) |

> 8/5 이전 항목(클래스 3종 확정 · ONNX 패키지 · AIHub 취득 경로 · 노면 마스킹 발견 ·
> `C4b`·`C7`·계단 야간/주간 분리)은 **[TODO 8장](TODO.md)** 으로 옮겼다. 결론은 위 1~4장에
> 이미 반영돼 있어, 여기서는 되풀이하지 않는다.
