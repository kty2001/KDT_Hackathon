# 밤마실 — 현재 상태 한 장

> **이 파일만 읽고 작업을 시작할 수 있게** 유지한다. 다른 문서는 *필요할 때만* 연다.
> 갱신: **2026-08-25** — 8/24 ④ **색 3종 확정** · ③ **ONNX 3클래스 계약 공지** 반영 ·
> **문서 최적화 2차**(이 파일 52.5→35KB · 인수분 원문 `archive/` 이관 · `W2` 기각 판정) ·
> 작업을 끝낼 때마다 이 파일을 먼저 고칠 것.

야맹증 저시력자의 야간 보행을 돕는 **실시간 AI 시각보조**(스마트폰 단독).
구조는 **표시/탐지 2경로**다 — 탐지는 **원본**을, 표시는 **①② 처리본**을 쓴다 (↓ 1장 다이어그램).
목표 **720p 15FPS**. 해커톤 마감 **9월 초**.

실행은 전부 `uv run python scripts/xxx.py` (→ [README](../README.md#개발-환경)).

---

## 1. 단계별 현재 상태

### ★ 아키텍처 확정 — 표시/탐지 경로 분리 (8/2, `C7`)

```
프레임 ─┬─→ [탐지 경로] 원본 그대로 ────────→ ③ YOLO ──┐
        │                                              ↓
        └─→ [표시 경로] ② D1A1+bf(+ts) ──────→ ④ 경계선 강조 → 화면
```

②를 탐지 앞단에 두면 mAP 가 내려가고, 무엇보다 **`stairs` 야간 오탐이 0.1% → 5.7%
로 되살아난다**(`C4b` 가 0 으로 만든 결함이 전처리 한 번에 복귀). 그래서 **탐지는 원본을
먹는다.** 분리는 비용이 아니라 이득이다 — 두 경로가 독립이라 병렬화 가능하고 ②의 지연이
③에 더해지지 않는다 (→ [detection.md 7장](detection.md)).

> ✅ **코드는 이미 이 구조다** (8/23 확인) — `scripts/pipeline_demo.py:128`
> `src = shown if args.fused else frame` 로 **탐지는 원본 프레임**을 받고, ②는
> 표시 경로에서만 돈다(`:120,:137`). 옛 직렬 구조(②→③)는 **`--fused` 플래그로만**
> 재현되며 기본 꺼져 있다(`:62-63`).
> ⚠️ **README·data.md 가 8/23 까지 폐기된 직렬 표기를 그대로 싣고 있었다** — 세 문서를
> 분기 표기로 통일했다. 파이프라인을 그림으로 옮길 때는 **이 절이 정본**이다.

| 단계 | 채택안 | 확정된 수치 | 상태 |
|------|--------|-------------|------|
| ① 눈부심 억제 | `D1`(Drago) 하이라이트 압축 — **표시 경로 전용** · **②에 흡수 예정** | 광원 코어 250→157 @640×360 | 🗣️ 통합 여부 회의 안건 |
| ② 저조도 개선 | **고전 CV 확정**(8/1) · **표시 경로 전용**. `D1A1+bf` + **`ts`(플리커)** | 3축 만족 · `+ts` 증분 **+1.5ms** | ⚠️ **속도 게이트 경계** (≈20.4ms@640×360) |
| ③ 위험요소 탐지 | **YOLO11n 단일 모델** · **입력은 원본** · 클래스 **`0 person` `1 stairs` `2 bollard`**(8/3 확정 · **8/22 회의 최종 확정**) · 🆕 **자체 3클래스 런 `c4e_s3_11n` 등장**(8/23) | **현 배포 `c4b_loli0`**(2클래스): held-out mAP50 0.684 · recall 0.609 · `stairs` 오탐 0.2% · **후보 `c4d_11n_640`**(인수분): 0.736 / 0.644 / 0.0% · 🆕 **`c4e_s3_11n`**(자체 3클래스): **0.710 / 0.635 / 0.0%** + 야간 볼라드 2/2 · 실야간 오탐 최저 | ✅ person·stairs 완료 (8/2) · ✅ **`c4d` held-out 판정 완료 (8/23) — 3런 통과, `c4d` 계보가 야간에서도 낫다** · 🟡 **채택 후보 2개 · 최종 선택은 `C5`** (→ [detection.md 9장](detection.md)) |
| ④ 선택적 강조 | 이중 스트로크 비채움 렌더 · **색 3종 확정**(8/24) | 박스당 **0.5ms** · `person` 시안 `#00E6FF` · `stairs` 노랑 `#FFD700` · 🆕 **`bollard` 라임 `#9CFF2E`** | ✅ 구현 · ✅ **색 확정 (8/24)** — 근거·후보 비교는 [노트북](../notebooks/emphasis_palette_review.ipynb) · 🔴 **`P1` 실기기 가독성 검증 대기**(라임↔노랑 적록색약 근접이 확인 대상) |
| **파이프라인 통합** | `scripts/pipeline_demo.py` | 데스크톱 end-to-end 동작 확인 (8/2) | ✅ 사전 검증 · 앱 이식 대기 |
| **③ 앱 전달물** | ONNX FP32 · 640 고정 · NMS 제외 | 🆕 **3클래스** `bammasil_det_c4e_s3_11n_640.zip` (9.20MB · 출력 **[1,7,8400]**) · PT↔ONNX **0.0001px** 일치 · 기존 2클래스 `c4b_loli0` 판(**[1,6,8400]**)도 유지 | 🔴 **계약 변경 — 앱팀 공지 완료** (8/24 · → [share 7-0](share_yolo_c4b_20260803.md)) |
| 앱 | — | — | ❌ **구현 0건** |

**채택 가중치**: `outputs/detect/c4b_loli0/weights/best.pt`
(`c4b_loli6000`(0.691/0.625/오탐 0.0%) 과 사실상 동점 — 학습 비용 절반 · pseudo-label
의존 없음. ⚠️ 요약 수치는 **채택 런 기준**으로 적을 것 — 두 런의 수치를 섞어 쓰면
"오탐 0.0%" 같은 과대 표기가 된다 → [detection.md 6-7](detection.md))

---

## ★ 외부 인수분 — `received/` (8/7 도착 · **판정 완료 8/23**)

> 번호 없는 절이다. 다른 문서가 `R1`~`R5` 로 참조하고 있어 **소절 제목을 유지**한다.
> 📦 **원문(6런 표 전체·오류 사례·경위)은
> [archive/received_c4d_2026-08-07.md](archive/received_c4d_2026-08-07.md)** 로 옮겼다(8/25).
> 여기는 **결정에 아직 필요한 것만** 남긴 요약이다.

별도 환경에서 돌린 **`C4d` — 3클래스 6런 비교**. 우리 `c4b_*` 와 겹치지 않는 **별개 계보**
(11K 축소셋)이고 **코드는 오지 않았다**(표·CSV·가중치만). 가중치는
`outputs/bammasil_results/bammasil_weights/c4d_*/best.pt` (git 비추적).

### R1. 6런 결과 — 공식 지표는 `benchmark_c4d.csv`

평가셋 `bollard_val_2146`(2,146장 · person 4,736 / bollard 6,988박스 · 누수 확인됨).
640 축 4런만 우리가 이어받았다(→ R4 · [detection.md 9장](detection.md)).

| 확정된 것 | 값 |
|---|---|
| **해상도가 모델 세대보다 크다** | 동일 50ep 에서 해상도 **+0.065** vs 모델 **+0.022**(3배) · 960 은 볼라드 FP 도 감소(925→792). ⚠️ 단 **person 오탐만은 오히려 는다** |
| **배경 이미지 10% 유지** | `noneg`(0%)는 mAP·recall 이 좋아 보이나 **6런 중 오탐 최다**(볼라드 +11% · person +16%) — **효과가 mAP 가 아니라 FP 에 난다.** mAP 만 보고 뺐으면 오판 |
| **운영 conf 무릎 0.10** | ⚠️ **주간 판단이다.** 야간에서 뒤집혀 **단일 conf 0.25 확정** (→ [detection.md 9-9](detection.md)) |
| **`<4px`(GT의 22%)** | 960 에서도 recall **0.49 가 상한** — 해상도로 더 못 간다(1280·타일링 영역) |

`stairs` 는 이 평가셋에 GT 0개라 6,988박스에서 **FP 2건**뿐. ⚠️ **주간 근거**이고
실야간에서는 뒤집힌다(2장 4). `confusion`(person↔bollard) 1% 미만 — **클래스 설계는 건전하다.**

### R2. ★ 이 수치는 전부 **주간**이다 — `is_night` 은 무효 필드

`is_night` 이 **파일 mtime**(추출·복사 시각)에서 나와(→ 3장 함정 13) `is_night=1` 로 고른
2,146장이 **99.1% 주간**이다. AIHub 실측 야간은 **0.58%**, 볼라드 보유 12,806장 중 야간 47장 ·
누수 없는 것은 **14장** — 통계가 성립하지 않는다.

> **→ 야간 볼라드는 인수분으로 영영 못 잰다.** NightOwls 에 볼라드 라벨이 없고 StairNet 은
> 계단 데이터셋이라, **`bollard` 의 야간 근거는 자체 촬영(`C2`)뿐이다.**
> ⚠️ 인수분은 **휘도만** 쟀으므로 `--dry-run` 의 **광원 연결성분**·**블록 수**는 남는다.

### R3. 기존 서술과 어긋난 것 — 전부 반영 완료

"AIHub 저조도 ~600장"은 **야간이 아니었다**(→ R2) · bollard 성능 전망은 방향이 맞았고
`<4px` 상한이 추가됐다 · **`C2` 우선순위는 내려가지 않고 올라갔다** · c4d 는 별개 계보라
`c4b` 를 자동 대체하지 않는다(→ R4). 대조표 원문은 archive.

### R4. ✅ 판정 완료 (8/23) — 그리고 **자체 3클래스 런이 더 낫다**

- ✅ 640 축 4런을 rec34 에 태웠다 — **3런 통과 · `26n_640` 기각 · 주간 순위가 야간에서
  뒤집혔다** (→ [detection.md 9장](detection.md))
- 🆕★ **`c4e_s3_11n`(자체)이 c4d 의 두 약점을 지웠다** — `stairs` 실야간 오탐(conf 0.10 에서
  21→**9박스**) · 야간 볼라드 confidence 붕괴(0.595→0.442→0.321 → **붕괴 없음** 0.759~0.804).
  차이는 c4d 에 없던 **NightOwls 실야간 + 배경 음성**이다 (→ [detection.md 11-3c](detection.md))
- ⚠️ 남는 것 — **코드가 없어 재현 불가** · 인수분 안에서도 `results.csv` ≠
  **`benchmark_c4d.csv`(공식값)** · 학습셋 11K 의 "야간 위주" 전제가 무효 필드 기반 ·
  **셋 다 seed 1개**라 최종 판정은 `C5`
- **실기기 속도 미측정** — 960 은 픽셀 2.25배. **배포 해상도는 640 유지**가 현재 판단이다

### R5. 회신 대기 3건 (→ `received/README.md` 10장)

1. 🔴 **야간 데이터셋 사양** — 장수·조건·평가전용 여부·클래스 범위. 보낸 쪽은 당일
   촬영·라벨링이 가능하다고 했다. **우리 `C2` 와 정면으로 겹친다 — 같은 밤을 두 번 찍지
   않도록 분업부터 정할 것.**
2. **ONNX 로 넘길 비교축 런 지정** — ⚠️ **YOLO26n 은 NMS-free 라 앱 인터페이스 계약이
   바뀐다**(raw 출력 + 앱측 NMS). 기존 배포물은 NMS 제외 FP32 640 이다
3. **KPI 비교표** — 실기기 640 계측(p50 32.7 · p95 38.1ms · 11분)이 나오면 960·26n 과 대조

---

## 2. 지금 막혀 있는 것 — 우선순위

1. 🔴 **`C2` 자체 야간 촬영** — `C3`→`C5`→`C7b`→`C8`→`C9` 가 전부 직렬로 매달려 있다.
   ✅ **선행 조건(회의 안건 3·4)은 8/3 해소** — 개인정보는 "안 걸리게 촬영", `stairs`
   라벨 정의는 확정(→ [labeling_stairs.md](labeling_stairs.md)). **막는 것이 없다.**
   - ✅ **마지막 전제였던 안건 7 도 8/22 에 종결됐다** — 클래스 **3종 최종 확정**이라
     촬영 코스에 장애물 구간이 추가될 일이 없다. **재촬영 위험이 사라졌다.**
     **이제 문서상 `C2` 를 막는 것이 하나도 없다.**
   - ⚠️ 촬영 시 **멀리서 계단에 접근하는 구간**을 반드시 넣을 것. 가까이서만 찍으면
     `stairs` 의 최대 미검증 항목(거리 계단)이 이번에도 측정 불가로 남는다.
   - 🆕 **볼라드 구간이 이제 필수다** (→ ★인수분 R2). AIHub 로는 야간 볼라드를 영영 못 잰다.
   - 🆕 **인수처도 야간 촬영을 하겠다고 회신을 기다리고 있다** (→ ★인수분 R5).
     같은 밤을 두 번 찍지 않도록 **사양·분업을 먼저 정할 것.**
   - 🆕🔴 **찍어 와도 지금은 판정을 못 한다 — `C5` 를 잴 하네스가 없다** (8/25 확인).
     `compare_detect.py` 는 NightOwls 전용 · `eval_real_night.py` 는 **라벨 없이 오탐만** 센다.
     라벨 있는 자체 야간분에서 후보 3개를 가르는 자가 **신설 대상**이고 **선행조건이 0** 이라,
     촬영을 기다리지 말고 먼저 만들어 둔다 (→ [TODO `C4e` S4](TODO.md)).
2. 🔴 **`P3` 앱 껍데기** — 구현 0건. `C9` 통합 시점에 없으면 통합할 곳이 없다.
3. ⚠️ **② 속도 게이트** — 720p 를 확실히 통과하는 건 `A1`·`A2`(6ms) 뿐인데 **이 둘은
   글레어 축 실격**이다. 채택안 `D1A1+bf+ts` 는 640×360 환산 **≈20.4ms** 로 게이트
   경계선(판정 불가 밴드 15~25ms). `bf` 를 A3 시간축으로 대체하려던 카드는 **8/2 에
   기각**됐다 (→ lowlight_classical 8장). 남은 수: 내부 해상도 하향 · 셰이더 이식 ·
   `C11` 실기기 재판정.
   - ✅ **다만 표시/탐지 분리로 압박이 줄었다** — ②의 지연이 ③에 더해지지 않으므로
     두 경로를 병렬로 돌리면 예산 계산이 달라진다. 🗣️ 회의 안건 1에 반영할 것.
   - 🆕 **③ 프레임 스킵 상한이 확정됐다 — `--detect-every` 2 까지** (8/23 `C4e` S1 E1).
     k=2→3 에서 오라클 대비 IoU **0.93→0.88** · miss **0.21→0.30** 으로 꺾인다.
     즉 **③ 예산을 절반으로 줄이는 것까지는 근거가 섰다** — 회의 안건 1의 입력이다
     (→ [detection.md 9-9-2](detection.md)).
4. **`stairs` — 저조도가 아니라 *도메인*이 문제였고, 실야간 오탐은 8/23 에 풀렸다**
   - 저조도는 문제가 아니다 — StairNet 야간 76장 mAP50 **0.993** · recall 0.987 로
     **주간보다 높다.** 그러나 StairNet 은 *계단이 화면을 채운 사진*이라 **보행 중 멀리
     있는 계단**을 측정할 수 없다 (→ [detection.md 8장](detection.md)).
   - 🔴 **실야간 노면 오탐은 진짜였고 정량화됐다** (8/13 정성 → 8/23 정량). 계단 없는
     음성 15프레임 conf 0.10 에서 `c4b_loli0`(**현 배포**) **22박스** · `c4d_11n_640` 21박스
     — **현 배포가 더 심하다.** 즉 모델 계보가 아니라 **StairNet 에 음성 표본이 없다**는
     데이터 문제다 (→ [detection.md 9-6](detection.md)).
   - ✅★★ **그 진단대로 풀렸다** — 배경 음성 1,163장을 넣은 `c4e_s3_11n` 이 같은 15프레임에서
     **22 → 9박스**(2.4배 감소) · 발화 14/15 → **6/15**. 인수분 최저였던 `11s`(10박스)보다도
     낫다 (→ [detection.md 11-3c](detection.md)). ⚠️ 표본 15프레임이라 판정은 `C5`.
   - 🔴 **남은 미검증은 원거리 계단 하나** — 야간 답은 `C2` 뿐이다. 주간은 AIHub 노면
     마스킹(**1인칭 보행 시점**)이 처음으로 잴 수단인데 **아직 안 받았다**(→ TODO `W5` ·
     [detection.md 8-3](detection.md)).
5. 🟡 **`bollard` — 데이터·배선·학습은 끝났고, 남은 것은 야간 판정 하나**
   - ✅ 클래스 **좁은 정의** 확정(8/22 · `pole`·`tree_trunk` 미흡수) · AIHub 3클래스 변환본
     **113,163장** 로컬 확보 · 배선(`aihub_subset_to_yolo.py`) 8/23 신설 · 자체 3클래스 런 완료
   - ✅★★ **야간 볼라드 confidence 붕괴가 사라졌다** — c4d 계보는 가까운 볼라드가
     0.595→0.442→**0.321** 로 무너졌으나 `c4e_s3_11n` 은 0.759→0.785→**0.804** 로
     어두워져도 안 무너진다(세 밝기 전부 2/2 검출).
     → **클래스별 conf 카드는 c4d 를 쓸 때만 필요하다** (→ [detection.md 11-3c](detection.md)).
   - 🔴 **그래도 여전히 미측정 항목이다** — 근거가 `test_real_data/_04` **한 장**이고 라벨이
     없어 recall·FP 를 못 센다. AIHub 로는 영영 못 잰다(→ R2). **`C2` 가 유일한 판정 수단.**
   - ⚠️ **볼라드는 박스별로 볼 것** — 장면 단위 `maxconf` 만 보면 먼 쪽(0.67~0.71)에 가려
     가까운 쪽의 붕괴가 안 보인다.
   - ⚠️ 남는 한계 2개 — **`<4px`(GT의 22%)는 960 에서도 recall 0.49 상한** · ghost FP
     (기둥·가로등·표지판 지주) **57~63%** 는 좁은 정의 확정으로 **영영 오탐**이라 배경 음성
     표본으로 줄일 대상이다 (→ R1).

---

## 3. ⚠️ 반복해서 발목을 잡은 함정 — 작업 전 반드시 확인

1. **개발 val 은 야간 실효 성능을 못 본다.** `C4b` 에서 실야간이 5.4배 좋아지는 동안
   개발 val 은 0.892 → 0.892 로 **한 자리도 안 움직였다.** 판정은 **항상**
   held-out(NightOwls rec 34) 또는 자체 촬영분(`C5`)에서 한다.
2. **지표를 영상 통계에 상대적으로 정의하지 말 것.** 백분위·평균 대비로 정의하면
   영상이 바뀔 때 *재는 대상 자체*가 달라진다. 노이즈·글레어·대비 3개 지표가 전부
   이 결함으로 육안과 어긋났다 (→ `scripts/metrics.py`).
3. **이 PC 는 15~25ms 를 판정할 수 없다.** 프로세스 간 10~20% 흔들린다.
   다른 작업(학습 등)과 동시에 재면 **1.7배까지 부풀어 오른다.**
4. **NightOwls `ignore`-only 프레임 3,097장은 학습에 넣지 말 것.** 사람이 있는데
   라벨이 비어 있어 "사람=배경"을 가르친다 (→ `scripts/nightowls_yolo.py`).
5. **NightOwls rec 34·38 은 학습 금지.** 34 는 판정용 held-out, 38 은 ② 시간축 전용.
   `build_detect_dataset.py` 가 실행을 거부하도록 막아 뒀다.
6. **Windows 에서 `--cache ram` 금지.** worker 가 spawn 이라 캐시가 복제돼 죽는다.
7. **Raw 데이터 수정 금지.** 산출물은 전부 `outputs/`(git 비추적), 이미지는 하드링크.
8. ★ **AI Hub 는 해외 IP 다운로드를 차단한다** (8/4 실측). Colab·해외 클라우드에서
   `aihubshell` 을 돌리면 `502 · "해외에서의 데이터 다운로드를 제한"` 으로 끝난다.
   우회는 이용정책 위반이라 하지 않는다 — **AIHub 취득은 국내 PC 에서만 가능**하고,
   클라우드 GPU 를 쓰려면 **국내에서 받아 640 리사이즈해 옮기는** 경로뿐이다
   (`scripts/aihub_pack_for_colab.py` → [data.md 3-1-3](data.md)).
   같은 실측에서 하나 더 — **라벨과 원천이 안 갈려 있다.** 태스크별 10GB 통zip 뿐이라
   "라벨 먼저 받아 분포 실측" 이 불가능하고, **태스크별 1번 zip 정찰**로 대체한다.
9. **ultralytics 의 AutoUpdate 를 켜 둔 채 ONNX 추론을 돌리지 말 것** (8/3 실측).
   `onnxruntime`(CPU) 이 없으면 **제멋대로 설치**해 `uv.lock` 밖 패키지를 끼워 넣는다.
   이 프로젝트는 `onnxruntime-gpu` 를 쓰는데 **둘은 같은 디렉토리를 공유**해서, 나중에
   CPU 판을 지우면 공용 파일까지 사라져 `onnxruntime` 이 통째로 망가진다
   (`module has no attribute 'InferenceSession'`). 복구는 `uv sync --reinstall-package
   onnxruntime-gpu`. 예방은 임포트 전 `YOLO_AUTOINSTALL=false` (→ `scripts/export_onnx.py`).
10. **`.pt` 와 ONNX 를 같은 이미지로 비교할 때 파일 경로를 그대로 넘기지 말 것** (8/3).
   ultralytics 는 `.pt` 에 **가변 rect 레터박스**(16:9 → 640×384), 고정 shape ONNX 에는
   640×640 을 먹인다. 입력 해상도가 달라 비정사각 이미지에서 신뢰도가 0.1 씩 벌어지는데
   이는 **변환 손실이 아니라 전처리 차이**다. 미리 정사각 레터박스한 배열을 양쪽에
   똑같이 넣으면 좌표 오차 0.0001px 로 일치한다.
11. ★ **다른 머신으로 옮길 데이터셋의 `data.yaml` 에는 `path:` 를 넣지 말 것** (8/4 실측).
    ultralytics 는 `path:` 가 **상대경로면 `settings['datasets_dir']` 기준**으로 푼다
    (Colab 은 `/content/datasets`). 로컬에서는 우연히 맞아떨어지지만 zip 을 옮기는 순간
    `images not found, missing path '/content/datasets/outputs/datasets/...'` 로 죽는다.
    절대경로를 넣으면 이번엔 Windows 경로가 박혀 역시 못 쓴다. **`path:` 를 빼면
    ultralytics 가 yaml 파일의 위치를 기준으로 잡아** 어느 머신에서든 동작한다
    (`aihub_pack_for_colab.make_portable`). 로컬 전용 데이터셋은 지금처럼 절대경로여도 된다.
12. ★ **한국어 Windows 에서 조용히 죽는 것 2개** (8/5 실측). 둘 다 **큰 데이터를 다 훑은
    뒤**에 터져서 실행이 통째로 날아간다 — 긴 작업 전에 확인할 것.
    - **콘솔이 cp949 라 `⚠️`·`★` 출력에서 `UnicodeEncodeError`.** 대비는
      `sys.stdout.reconfigure(errors="replace")` (한글은 그대로, 이모지만 흘린다).
      `chcp 65001` 을 요구하지 않는 쪽이 낫다 — 팀원이 한 번 돌리는 스크립트다.
    - **`cv2.imread` 가 비ASCII 경로를 못 연다.** `data/AIHub인도보행영상…` 처럼 한글이
      섞이면 예외 없이 **전부 `None`** 이 되어 "한 장도 못 읽었다"로 끝난다. 대비는
      `cv2.imdecode(np.fromfile(path, np.uint8), ...)`. PIL 은 영향 없다.
13. ★ **파일 mtime 을 촬영시각으로 쓰지 말 것** (8/7, 외부 인수분에서 발생 → ★인수분 R2).
    AIHub 야간 선별이 `mtime` 기반 `is_night` 으로 돌아가 **99.1% 가 주간인 셋을 "야간"으로**
    쓰고 있었다. mtime 은 추출·복사 시각이고 EXIF 촬영시각은 아예 없다.
    **야간 판정은 실측 휘도로 한다** — 이 절 2번과 뿌리가 같다(재는 대상이 딴것이 된다).
14. ★ **`outputs/datasets/` 의 파생 평가셋은 하드링크가 아니라 *복사본*이다** (8/22).
    **원본은 전부 `D:` 이고 `outputs/` 는 C:** 라(→ 아래 15번) `os.link` 가 볼륨을 넘지
    못해 `link_or_copy` 가 통째로 복사한다 — 장당 ~1MB PNG 라 금방 수십 GB 가 된다.
    `eval_nightowls.py`·`eval_stairs_night.py` 양쪽에 해당한다.
    **전부 재생성 가능한 산출물**이므로 C: 여유가 부족하면 지우고 필요할 때 되살린다.
    ```powershell
    uv run python scripts/eval_nightowls.py --dst outputs/datasets/nightowls_eval  # 해제분 전체
    uv run python scripts/eval_nightowls.py --recordings 38 --fp-only              # rec38
    ```
    ⚠️ `nightowls_eval` 은 **레거시 경로**다 — 현재 기본 dst 는 `nightowls_split/all` 이라
    `--dst` 를 명시하지 않으면 다른 곳에 만들어진다.
15. ★ **데이터셋 실체는 전부 `D:\datasets\` 다 — Junction 이 없으면 스크립트가 죽는다**
    (8/22 재편 · **8/23 복구 완료**). `data/<이름>` 을 하드코딩하는 스크립트가 7개라
    (`stairnet_to_bbox` · `night_eval` · `inspect_datasets` · `compare_lowlight` ·
    `metrics` · `probe_classical` · `build_detect_dataset`(LoLI)) Junction 이 빠지면
    **"데이터 없음"으로 조용히 끝난다.** 저장소를 새로 받으면 **Junction 부터 걸 것**
    (관리자 권한 불요):
    ```powershell
    foreach ($n in "NightOwls","LoLI-Street","Stair dataset","ExDark","LOLdataset") {
        if (-not (Test-Path "data\$n")) {
            New-Item -ItemType Junction -Path "data\$n" -Target "D:\datasets\$n"
        }
    }
    ```
    ⚠️ **AIHub 만 예외** — `D:\datasets\bammasil_aihub_subset` 을 **`--src` 인자로** 넘긴다.
    ★ 받는 쪽은 `aihub_to_yolo.py` 가 **아니다**(CVAT XML 전용) — 이 서브셋은 이미 YOLO
    변환본이라 **`aihub_subset_to_yolo.py`**(8/23 신설)가 받는다.
    회귀 확인은 `uv run python scripts/inspect_datasets.py` 한 방 (→ [data.md 5-2](data.md)).
16. ★ **2클래스 → 3클래스로 갈아탈 때 조용히 깨지는 것 — 3개 중 2개 해소**
    (8/23 발견 · **8/24 해소**). 현 배포는 아직 2클래스 `c4b_loli0` 이고 채택 후보는 3클래스다.
    - ✅ **④ `bollard` 색** — `person` 시안 `#00E6FF` · `stairs` 노랑 `#FFD700` ·
      **`bollard` 라임 `#9CFF2E`**. `FALLBACK_COLOR`(흰색)가 이제 **세 클래스 어느 것도 아닌
      값**이라 미지정 클래스가 볼라드로 오인될 여지가 사라졌다. 고른 자는
      [`emphasis_palette_review.ipynb`](../notebooks/emphasis_palette_review.ipynb)
      — `CANDIDATES` 한 곳만 고치면 재실행되고, **렌더 로직은 `emphasize.py` 한 곳**에 그대로 둔다.
      - ★★ **한 색이 두 축을 다 이기지 않는다.** 라임은 **배경 대비**(4.18~10.05 · 모든
        밝기에서 WCAG 3:1 통과)를 산 선택이고, 대가는 **적록색약에서 `stairs` 노랑과
        가까워지는 것**(녹색맹 최소 ΔE **9.9** · 적색맹 20.5)이다.
        🔴 **이 절충이 `P1` 의 판정 대상**이며, 되돌아갈 자리 `violet`(#C77DFF)·
        `magenta`(#FF3CDC)는 **밝기 대비가 3:1 아래**라는 반대급부를 안는다.
      - ⚠️ **세 클래스가 동시에 있는 실사진이 로컬에 없다** — `test_real_data` 는 **사람 0장**,
        AIHub bbox 에는 **`stairs` 0건**이라 노트북이 합성 박스 절과 AIHub 절로 나눠 낸다.
        🗣️ 보행 시점 야간에 사람이 찍힌 소재가 없다는 것은 **`C2` 코스 요구 후보**다.
    - ✅ **ONNX metadata 의 held-out 지표** — `c4e_s3_11n` 을 `RUN_METRICS` 에 등록 후 내보냈다.
      같은 수정에서 **미측정(rec38)을 `0.0%` 로 보이지 않게** README 렌더도 고쳤다 — 안 잰 것과
      0 인 것은 다르다. ⚠️ c4d 를 내보낼 일이 생기면 **그때도 먼저 등록**할 것.
    - 🔴 **남은 하나 — 배포 계약의 conf 가 단일 스칼라다** (`export_onnx.py:531`).
      클래스별 conf 를 채택하면 이 필드가 dict 로 바뀌고 README 후처리 절차(`:299`·`:318`)도
      같이 고쳐야 하며 **앱팀 공지 대상**이다. 🆕 **단 `c4e_s3_11n` 을 쓰는 한 손댈 일이 없다**
      (야간 볼라드 conf 0.759~0.843 이라 0.25 여유가 두껍다 → [detection.md 11-3c](detection.md)).
    - ✅ 출력 채널 **2 → 3** 변경과 ④ 색은 **8/24 에 앱팀 공지 완료**
      (→ [share 7-0](share_yolo_c4b_20260803.md)).
17. ★★ **세션 키에 '그룹'을 넣지 말 것 — 누수가 "누수 0" 으로 보고된다** (8/23 실측).
    `bammasil_aihub_subset` 의 그룹 폴더(`bollard_night`·`bollard_day`·`negative_night`·
    `person_night`)는 무효 필드 `is_night`(→ 함정 13) 기반이라 **같은 촬영 세션이 그룹마다
    쪼개져 있다** — 전체 블록 2,296개 중 **1,357개(59%)가 여러 그룹에 걸친다.**
    분할 키를 `(그룹, 블록)` 으로 잡으면 같은 세션이 train 과 val 로 갈리는데 **검사도 그
    키로 하므로 "세션 누수 0" 이라고 보고된다.** 실측 피해는 **val 2,205장 중 568장(26%) 오염.**
    ⚠️ 개발 val 만 부풀어 오르는 게 아니다 — `best.pt` 는 **val fitness 로 선택**되므로
    **모델 선택 자체가 왜곡된다.**
    → **블록(세션)을 그룹과 무관한 원자 단위로 다룬다.** 검사는 반드시 **산출된 데이터셋
    파일명에서 되짚어** 한다 — 선별 로직이 쓰는 키로 검사하면 같은 버그를 못 잡는다.
    `scripts/aihub_subset_to_yolo.py` 가 이 규칙으로 되어 있다.

---

## 4. 데이터 — 무엇이 어디에

| 데이터셋 | 규모 | 성격 · 주의 |
|---|---|---|
| **NightOwls** | 51,848장 중 **13,602장 해제** | ★ 사람 손 라벨 · **전량 야간**. 유일한 정직한 야간 평가원. **차량 대시캠**이라 보행 시점과 다름 |
| LoLI-Street | 33,000쌍 (+person 라벨) | ⚠️ **주간을 어둡게 만든 합성본.** 포화 광원 **0.00%** → 글레어를 학습·측정 불가 · 노이즈가 `high` 보다 **적다**(σ 0.97 vs 7.13 — 실야간이면 정반대) · low/high 라벨 불일치(high 가 +24.8%). ★ **합성 증강(`W2`)은 8/25 에 기각** — 넣든 빼든 실야간 동점 (→ [detection.md 6-7](detection.md)) |
| StairNet | 3,094장 (야간 505) | `stairs` 유일 소스. **계단 중심 사진**이라 음성 표본이 없음 |
| ExDark | 7,363장 | 실제 야간이나 **실내가 많음** |
| LOL | 500쌍 | 벤치마크 비교용 |
| **AIHub 인도보행** | bbox 전량 300GB 확보(8/5) | `bollard` 유일 소스 · **주간 데이터셋**(실측 야간 **0.58%** — 8/7 확정, → ★인수분 R2) |

> 📍 **실체는 전부 `D:\datasets\` 다** (8/22 재편 · 용량 이슈). 저장소 기준 경로는
> `data/<이름>` 으로 통일돼 있고 그것이 **Junction** 이다 — 스크립트에 경로 분기가 없다.
> ✅ **5종 전부 연결돼 있다** (8/23 복구). 저장소를 새로 받으면 Junction 부터 걸 것
> — 명령과 확인법은 → 3장 함정 15 · [data.md 5-2](data.md).
> AIHub 만 예외로 `D:\datasets\bammasil_aihub_subset` 을 `--src` 인자로 넘긴다.
> ⚠️ **단 `aihub_to_yolo.py` 는 CVAT XML 전용이라 이 서브셋을 못 받는다** (8/23 정정).
> 이 서브셋은 **3클래스 YOLO 변환본 113,163장**이고, 학습에 넣으려면 다리가 하나 필요하다
> (→ [detection.md 11장](detection.md)).

**NightOwls recording 배치 (고정)**: train `36·none·37` / val `35` / **test `34`** / 제외 `38`

**실촬영 테스트 소재**: `data/test_real_data/` (야간 실촬영 사진 7 + 동영상 5, 8/8 · 세로
3060×4080). `lowlight/`(gain 0.35) · `lowlight_x02/`(gain 0.2) 는 `scripts/darken.py` 로
**한 단계씩 더 어둡게 합성**한 것(선형 노출 감소 + 센서 노이즈) — ② arm 의 하한을 눈으로
보는 **정성 확인용**. 합성본이므로 지표 근거로 쓰지 말 것.
★ **라벨은 없지만 내용이 값나간다** — `_04` **야간 볼라드 2개**(프로젝트 유일) · `_05` 계단 ·
나머지 5장은 계단·볼라드가 **없는** 야간 노면이라 **오탐을 볼 음성 표본**이다. **사람은 0장.**

---

## 5. 문서 지도 — *필요할 때만* 연다

| 알고 싶은 것 | 문서 | 크기 |
|---|---|---|
| **지금 뭘 해야 하나** (순서·병렬성·일정·담당) | [TODO.md](TODO.md) | 35KB |
| ★ **③ 탐지 전부** — `C4`·`C4b` 판정(3~6장) · 계단 도메인(8장) · **`C4e` S0~S3**(9·11장) · **`W2` 합성 증강 기각**(6-7) | [detection.md](detection.md) | 94KB |
| ★ **앱팀 인수인계** — 🔴 3클래스 계약(출력 2→3) · ④ 색 · 연동 주의 | [share 7-0](share_yolo_c4b_20260803.md) | 15KB |
| ② 고전 arm — 지표·순위·속도·A3 시간축 | [lowlight_classical.md](lowlight_classical.md) | 51KB |
| 데이터 상세 · **AIHub**(3-1) · 계단 방식 근거 · 데이터 위치(5-2) | [data.md](data.md) | 61KB |
| 기획 (배경·구성·일정) · 환경 셋업 | [../README.md](../README.md) | 24KB |
| 모바일 런타임·양자화·FPS 병목 · 프레임 스킵 | [hardware_inference.md](hardware_inference.md) | 9KB |
| 스크립트가 뭘 하는지 | [../scripts/README.md](../scripts/README.md) | 7KB |
| ★ **`stairs` 라벨을 어떻게 치는가** (`C3` 작업자용) | [labeling_stairs.md](labeling_stairs.md) | 10KB |
| ★ **기획서 v20 수치 검증** — 수정 필요 9건 (미반영) | [review_proposal_v20](review_proposal_v20_20260808.md) | 19KB |
| 외부 인수분 1차 자료 (회신 요청 10장) | [../received/README.md](../received/README.md) | 15KB |
| 종결된 실험·결정 원문 — **인수분 `C4d` 원문 포함** | [archive/](archive/) | — |

**노트북 — 판정을 그림으로 확인할 때**

| 보고 싶은 것 | 노트북 |
|---|---|
| ④ `bollard` 색을 어떻게 골랐나 (색각이상 × 배경 대비) | [emphasis_palette_review](../notebooks/emphasis_palette_review.ipynb) |
| c4d 라벨 검수 · 640 추론 · **실야간 추론** | [aihub_subset_review](../notebooks/aihub_subset_review.ipynb) |
| 자체 3클래스 런 `c4e_s3_11n` 추론 | [c4e_infer](../notebooks/c4e_infer.ipynb) |
| 계단 탐지가 완료됐는가 | [stairs_night_review](../notebooks/stairs_night_review.ipynb) |
| AIHub 를 받아 학습까지 (GPU 없는 PC 에서도) | [colab_aihub_train](../notebooks/colab_aihub_train.ipynb) |
| ② arm 육안 비교 (야간 표본 · LOL paired) | [lowlight_arms_review](../notebooks/lowlight_arms_review.ipynb) · [lowlight_lol_review](../notebooks/lowlight_lol_review.ipynb) |

> 📌 **큰 문서는 통째로 읽지 말 것.** 각 문서 맨 위 `0. 결론 요약`만 읽고,
> 필요한 절만 `grep -n "^#" 파일` 로 찾아 `sed -n 'A,Bp'` 로 부분 열람한다.
> 목적 축을 **무엇으로 어떻게 재는가**는 [`scripts/metrics.py`](../scripts/metrics.py) 가 정본이다.

---

## 6. 최근 완료 (최근 며칠)

전체 완료 이력은 → [TODO 8장](TODO.md). 여기는 **아직 머릿속에 있어야 하는 것**만 둔다
— 결론은 이미 1~4장에 반영돼 있으므로 **한 줄로만** 적는다.

| 항목 | 결과 | 원문 |
|---|---|---|
| ★ **④ 색 3종 확정 · ③ ONNX 3클래스 공지** (8/24) | `bollard` 라임 `#9CFF2E` 채택(대비를 산 선택 · `P1` 이 판정) · 출력 계약 `[1,6,8400]`→`[1,7,8400]` · `RUN_METRICS` 등록 | 3장 함정 16 · [share 7-0](share_yolo_c4b_20260803.md) |
| ★★★ **`C4e` S3 — 첫 자체 3클래스 런 `c4e_s3_11n`** (8/23) | `detect_v3`(16,291장) 100ep. **게이트 3종 전부 통과** — recall 0.609→**0.635** · `stairs` 야간 오탐 0.2→**0.0%** · 실야간 음성 6→**5박스**. ★ 실야간 비중이 100%→42.2% 로 희석됐는데도 recall 이 올랐다(**실촬영** 주간이라 합성으로 일반화 금지) | [detection.md 11-3c](detection.md) |
| ★★ **`C4e` S2 — 배경 음성 보강** (8/23) | AIHub `negative_night` 1,163장 투입 → `stairs` 실야간 오탐 conf 0.10 에서 **22→9박스**. 배선은 신설 `aihub_subset_to_yolo.py`(누수 결함 1건 동시 발견 → 함정 17) | [detection.md 11-3b](detection.md) |
| ★★ **`C4e` S1 — 추론측 두 실험** (8/23) | **단일 conf 0.25 확정**(일괄 하향 **기각** — 시간축이 오탐 지속을 13.6→24.7프레임으로 **늘린다**) · 클래스별 `bollard` 0.15 는 `11s` 에서만 이득 · `W6`·`P1` **값 없음** · `--detect-every` 상한 **2** · ★ 깜빡임의 주범은 스킵이 아니라 **탐지기 자체** | [detection.md 9-9](detection.md) |
| ★★ **`C4e` S0·S0b — `c4d` held-out 판정** (8/23) | 640축 4런 → **3런 통과 · `26n_640` 기각**. ★★ **주간 순위가 야간에서 뒤집혔다**(함정 1 의 세 번째 증거) · S0b 는 라벨 없이 오탐을 셌고 **세 축의 1위가 전부 다르다** | [detection.md 9장](detection.md) |
| 🗣️ **8/22 회의 — 클래스 3종 최종 확정** | `0 person`·`1 stairs`·`2 bollard` 고정 · `bollard` **좁은 정의**(`pole`·`tree_trunk` 미흡수) · 나머지 26종 미채택 · 노면 20종은 마감 후. **`C2` 를 막던 마지막 전제 소멸** | 5장 안건 7(TODO) |

> **그 이전(8/5~8/23)은 전부 [TODO 8장](TODO.md) 으로 옮겼다** — 문서 정합 정리 ·
> `data/` Junction 5개 재연결 · `C4e` 계획 수립 · `D:` 데이터 재편 · `outputs/datasets`
> 18.3GB 회수 · c4d 육안 검수 · 기획서 v20 검증 · 인수분 도착 · 저조도 정찰.
> 결론은 위 1~4장에 이미 반영돼 있어 여기서 되풀이하지 않는다.
